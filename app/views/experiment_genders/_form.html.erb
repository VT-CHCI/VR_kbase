<%= form_for(@experiment_gender) do |f| %>
  <% if @experiment_gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment_gender.errors.count, "error") %> prohibited this experiment_gender from being saved:</h2>

      <ul>
      <% @experiment_gender.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="field">
    <%= f.label :experiment %><br />
    <%= f.text_field :experiment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
