<%= form_for(@task_scale) do |f| %>
  <% if @task_scale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_scale.errors.count, "error") %> prohibited this task_scale from being saved:</h2>

      <ul>
      <% @task_scale.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :scale %><br />
    <%= f.text_field :scale %>
  </div>
  <div class="field">
    <%= f.label :task %><br />
    <%= f.text_field :task %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
