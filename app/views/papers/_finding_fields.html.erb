<div id="finding_new_findings" class="finding core-element" data-experiment="new_experiments" data-task="new_tasks" data-finding="new_findings">

  <h1>Finding Information</h1>

  <p><small>This section documents the findings from the user task in the experiment. For each finding you will begin by providing the basic details, and then add relationships between the components of fidelity a metric for a particlar task category.</small></p>

  <legend class="with-helper-text">General Information</legend>
  <p class="after-legend"><small>Create a title / nickname for the finding so that it can be referred back to easily in the entry progress list.</small></p>

  <div class="field field-title">
    <%= f.label :title, "Finding Title" %>
    <%= f.text_field :title, :class => 'span12' %>
  </div>

  <legend class="with-helper-text">Finding Information</legend>
  <p class="after-legend"><small>This area provides the details of the finding. Choose one or more of components of fidelity and the relationship between a metric for a particular task.</small></p>

  <div class="field">
    <%= f.label nil, "Components of Fidelity" %>
    <div class="fidelity auto-gen-field">

    </div>
  </div>

  <div class="field">
    <%= f.label :relationship %>
    <% Relationship.all.each do |relationship| %>
      <label class="radio inline pill relationships auto-gen-field">
        <%= f.radio_button :relationship_id, relationship.id %>
        <%= f.label :relationship_id, relationship.relationship, :value => relationship.id %>
      </label>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :metric %>
    <div class="metrics auto-gen-field">

    </div>
  </div>

  <div class="field">
    <%= f.label :task %>
    <div class="tasks auto-gen-field">

    </div>
  </div>

  <div class="field field-summary">
    <%= f.label :summary, "Finding Summary" %>
    <%= f.text_field :summary, :class => 'span12' %>
    <span class="help-block">This summary is auto generated by selections made above. Feel free to change it, but please keep it succinct.</span>
  </div>

  <div class="field field-notes">
    <%= f.label :finding_note, "Finding Details" %>
    <%= f.text_area :finding_note, :class => 'span12', :rows => 5 %>
  </div>

  <legend class="with-helper-text">Additional Information</legend>
  <p class="after-legend"><small>This last area is for documenting the specificity of the finding and any issues / confounds that are pertinant.</small></p>

  <div class="field">
    <%= f.label :specificity, 'Specificity of Finding' %>
    <% Specificity.all.each do |specificity| %>
      <label class="radio inline pill">
        <%= f.radio_button :specificity_id, specificity.id %>
        <%= f.label :specificity_id, specificity.level, :value => specificity.id %>
      </label>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :issue, "Issues" %>
    <%= f.text_area :issue, :class => 'span12', :rows => 5 %>
  </div>

  <div class="field">
    <button type="button" class="btn btn-primary" onclick="click_add_finding(new_tasks); return false;">Next Step -> Add Another Finding</button>
  </div>

  <div class="field">
    <button type="button" class="btn btn-primary" onclick="click_add_task(new_experiments); return false;">Next Step -> Add Another Task</button>
  </div>

  <div class="field">
    <button type="button" class="btn btn-primary" onclick="click_add_experiment(); return false;">Next Step -> Add Another Experiment</button>
  </div>
</div>