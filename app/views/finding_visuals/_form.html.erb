<%= form_for(@finding_visual) do |f| %>
  <% if @finding_visual.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finding_visual.errors.count, "error") %> prohibited this finding_visual from being saved:</h2>

      <ul>
      <% @finding_visual.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :finding %><br />
    <%= f.text_field :finding %>
  </div>
  <div class="field">
    <%= f.label :visual_fidelity %><br />
    <%= f.text_field :visual_fidelity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
