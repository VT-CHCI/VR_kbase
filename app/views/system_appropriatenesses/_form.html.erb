<%= form_for(@system_appropriateness) do |f| %>
  <% if @system_appropriateness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_appropriateness.errors.count, "error") %> prohibited this system_appropriateness from being saved:</h2>

      <ul>
      <% @system_appropriateness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component %><br />
    <%= f.text_field :component %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
