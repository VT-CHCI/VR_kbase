<%= form_for(@specificity) do |f| %>
  <% if @specificity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specificity.errors.count, "error") %> prohibited this specificity from being saved:</h2>

      <ul>
      <% @specificity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
