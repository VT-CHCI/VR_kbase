<%= form_for(@task_metric) do |f| %>
  <% if @task_metric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_metric.errors.count, "error") %> prohibited this task_metric from being saved:</h2>

      <ul>
      <% @task_metric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :metric %><br />
    <%= f.text_field :metric %>
  </div>
  <div class="field">
    <%= f.label :task %><br />
    <%= f.text_field :task %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
