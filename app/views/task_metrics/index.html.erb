<h1>Listing task_metrics</h1>

<table>
  <tr>
    <th>Metric</th>
    <th>Task</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @task_metrics.each do |task_metric| %>
  <tr>
    <td><%= task_metric.metric %></td>
    <td><%= task_metric.task %></td>
    <td><%= link_to 'Show', task_metric %></td>
    <td><%= link_to 'Edit', edit_task_metric_path(task_metric) %></td>
    <td><%= link_to 'Destroy', task_metric, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task metric', new_task_metric_path %>
