<%= form_for(@control_symmetry) do |f| %>
  <% if @control_symmetry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@control_symmetry.errors.count, "error") %> prohibited this control_symmetry from being saved:</h2>

      <ul>
      <% @control_symmetry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component %><br />
    <%= f.text_field :component %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
