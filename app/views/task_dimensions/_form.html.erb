<%= form_for(@task_dimension) do |f| %>
  <% if @task_dimension.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_dimension.errors.count, "error") %> prohibited this task_dimension from being saved:</h2>

      <ul>
      <% @task_dimension.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dimension %><br />
    <%= f.text_field :dimension %>
  </div>
  <div class="field">
    <%= f.label :task %><br />
    <%= f.text_field :task %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
